{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": [
        "supabase-mcp"
      ],
      "env": {
        "SUPABASE_URL": "YOUR_SUPABASE_URL",
        "SUPABASE_SERVICE_ROLE_KEY": "YOUR_SERVICE_ROLE_KEY",
        "SUPABASE_ANON_KEY": "YOUR_ANON_KEY",
        "MCP_API_KEY": "placeholder"
      }
    }
  }
}

// NOTE: This file is tracked in git with placeholder values.
// The actual configuration with real keys is in mcp.local.json (gitignored).
// Copy this file to mcp.local.json and replace the placeholder values with your actual Supabase credentials.