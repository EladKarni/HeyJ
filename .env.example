# Example environment variables for HeyJ
# Copy this file to .env and fill in your secrets

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# SECURITY WARNING: Service role keys should NEVER be used client-side
# Server-only operations use separate .env files
# For E2E tests, use test-specific environment variables

# OneSignal Configuration (Client-side)
# These are used by the React Native app
EXPO_PUBLIC_ONESIGNAL_APP_ID=
EXPO_PUBLIC_ONESIGNAL_REST_API_KEY=

# NOTE: Server-side OneSignal credentials for Supabase Edge Functions
# must be set separately using:
# npx supabase secrets set ONESIGNAL_APP_ID="your-app-id"
# npx supabase secrets set ONESIGNAL_REST_API_KEY="your-rest-api-key"