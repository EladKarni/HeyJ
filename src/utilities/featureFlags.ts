/**
 * Feature flags for HeyJ app
 * Toggle features on/off from here
 */

export const featureFlags = {
  // Hide recipient selector in ConversationScreen since recipient is already determined
  // Set to false to allow changing recipient in conversation screen
  hideRecipientInConversationScreen: true,
};
